/* Eine Stelle für die Build-Kennung. Muss zum CACHE_NAME in sw.js passen —
   beide zusammen hochzählen, sonst zeigt die App eine andere Version an,
   als der Service Worker ausliefert. */
export const APP_BUILD = 'v8.38';
export const APP_BUILD_DATE = '2026-08-26';
